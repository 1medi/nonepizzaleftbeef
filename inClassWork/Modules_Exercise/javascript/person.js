export var Person = (first, last, age) => {
  return "My name is " + first + " " + last + " and I am " + age + " years old.";
}
 
/**
 * @param exports the functions first last and age to the formatted.js file
 * @return reads the sentence using the values from the formatted.js file and returns it to the entire index.html
 * @desc exporting a function for formatted.js to read, giving a value, then returning the values to index.html
 */

